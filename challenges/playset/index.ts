const playset = (s: string) => {
  // If the string s contains duplicates characters, your function should return true, if it does not, it should return false.
  // Examples:
  // abcde     false
  // abbcdde     true
  // abcdeab     true
};

export default playset;
