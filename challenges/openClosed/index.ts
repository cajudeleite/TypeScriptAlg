const openClosed = (s: string) => {
  // Write a function that receives as first parameter a string containing any combination of parentheses, brackets or quotes (single or double).
  // Your function will have to make sure that each "opening" character of the string has a corresponding "closing" character located after it in the string (and conversely: each "closing" character must have a corresponding "opening" character located before it).
  // If so, it must return true. If not, it must return false.
  // Examples:
  // ()     true
  // ][     false
  // )""     false
  // (')'     true
};

export default openClosed;
