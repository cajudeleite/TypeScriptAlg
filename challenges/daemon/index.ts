const daemon = (numbers: number[], k: number) => {
  // Your function will have to determine if the array is partitioned and if the element at index k is a pivot.
  // If so, it must return true. If not, it must return false.
  // An array is partitioned with a pivot at index k if and only if:
  //  - all elements preceding index k in the array are strictly inferior to the value at index k
  //  - all elements following index k in the array are greater than or equal to the value at index k
  // Examples:
  // [100, 97, 101, 109, 111, 110], 1     false
  // [100, 97, 101, 109, 111, 110], 2     true
  // [100, 97, 101, 109, 111, 110], 3     true
};

export default daemon;
